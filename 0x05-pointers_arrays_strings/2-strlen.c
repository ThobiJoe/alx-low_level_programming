#include "main.h"
#include <string.h>
/**
 * _strlen - Return length of string
 * @s: String to be checked
 * Return: integer
 */
int _strlen(char *s)
{
	size_t length = strlen(s);

	return (length);
}
