#include <stdio.h>
/**
 *main - Entry point
 *Description: Print all letters of the alphabet except q and e
 *Return: 0 always
 */
int main(void)
{
	int n;

	n = 122;
	while (n >= 97)
	{
		putchar(n);
		n--
	}
	putchar('\n');
	return (0);
}
