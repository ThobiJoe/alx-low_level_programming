#include <stdio.h>
/**
 *main - Entry Point
 *Description: Print out alphabets in small case
 *Return: 0 always
 */
int main(void)
{
	int ch;

	for (ch == 97; ch <= 122; ch++)
	{
		putchar(ch);
	}
	return (0);
}
